# HidePartyFrameTitle

Hides the "Party" label that appears above the compact party frame in the World of Warcraft interface.

## Installation

Extract the `HidePartyFrameTitle` folder into your `Interface\AddOns` directory.
